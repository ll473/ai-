# AI 智能商城设计系统

## Design Read

Reading this as: 面向普通消费者与商城运营人员的可信电商产品，以克制、清晰、可解释的现代产品语言为主，用户商城重视商品视觉和购买路径，管理后台重视信息密度与操作效率。

参考规范：

- Taste Skill 用于商城首页、商品列表和详情页的布局节奏与反模板检查。
- Impeccable 用于产品界面、表格、表单、状态、可访问性和有界视觉验收。

不复制两个参考项目自身的品牌皮肤。

## Design Dials

### 用户商城

- DESIGN_VARIANCE: 6
- MOTION_INTENSITY: 3
- VISUAL_DENSITY: 5

### 管理后台

- DESIGN_VARIANCE: 4
- MOTION_INTENSITY: 2
- VISUAL_DENSITY: 7

## 品牌与反例

品牌性格：可信、聪明、直接、克制。

反例：

- 紫蓝渐变、霓虹光晕、玻璃拟态和发光粒子组成的“AI 风”。
- 每个内容块都放进白色圆角卡片。
- 虚构销量、评分、折扣和运营指标。
- 大量装饰性图标方块、状态圆点和小标签。
- 过度动画、页面滚动劫持和无意义悬浮位移。

## 颜色

全站使用浅色主题，商城与管理后台共享同一套语义令牌。

- `--color-brand-600: #1859d8`：主操作、选中状态、链接。
- `--color-brand-700: #1247b1`：主操作 hover。
- `--color-brand-50: #eef4ff`：低强调品牌背景。
- `--color-ink-950: #111827`：主标题与关键数值。
- `--color-ink-700: #374151`：正文。
- `--color-ink-500: #6b7280`：辅助说明。
- `--color-line: #dfe3e8`：边框与分隔线。
- `--color-surface: #ffffff`：主表面。
- `--color-ground: #f5f7fa`：页面背景。
- `--color-success: #137a4b`：成功、可售、运行完成。
- `--color-warning: #a15c00`：库存不足、需要关注。
- `--color-danger: #c0362c`：失败、危险操作。

主强调色只有钴蓝。成功、警告和错误色只能表达语义。

## 字体

- 中文与正文：`Noto Sans SC`, `PingFang SC`, `Microsoft YaHei`, system-ui。
- 数字：继承正文并使用 `font-variant-numeric: tabular-nums`。
- 不从公共 CDN 加载字体。
- 页面标题：32-44px，600-700 weight，行高 1.18。
- 区块标题：22-28px，600 weight，行高 1.3。
- 正文：14-16px，400 weight，行高 1.7。
- 表格与辅助信息：13-14px。

## 间距与布局

- 基础间距单位：4px。
- 常用间距：8、12、16、24、32、48、64px。
- 商城最大内容宽度：1440px。
- 管理后台内容最小留白：24px，宽屏为 32px。
- 桌面导航高度：68px。
- 多栏布局在小于 768px 时显式折叠为单栏。

## 形状、边框与阴影

- 输入框、按钮：8px 圆角。
- 普通容器、商品卡片、弹窗：12px 圆角。
- 标签：6px 圆角，不默认使用胶囊形。
- 边框：1px `--color-line`。
- 仅弹层、悬浮导航和明确的层级变化使用阴影。
- 普通卡片使用边框与留白，不使用黑色重阴影。

## 组件规则

### Button

- 高度 36px 或 40px。
- 主按钮使用品牌色实底。
- 次按钮使用白底加中性边框。
- 危险按钮只用于真实危险操作。
- 标签不换行，按下状态缩放不超过 0.98。

### Form

- 标签位于输入框上方。
- 帮助文字紧随标签或输入框。
- 错误信息位于输入框下方。
- placeholder 不承担标签职责。

### Card

- 只有商品、摘要实体或需要独立点击区域的内容使用卡片。
- 管理后台普通分组优先使用区块标题和分隔线。

### Table

- 表头固定使用中性浅背景。
- 数值列右对齐，操作列位置稳定。
- 加载、空结果、错误和无权限状态完整。

### Product Image

- 商品图使用真实上传资源。
- 缺图时显示明确的无图片状态，不生成假商品图。
- 列表图比例统一为 1:1，详情主图使用 4:5 或 1:1。

## 动效

- 页面与区块进入：160-220ms 淡入和 8px 以内位移。
- 按钮与输入反馈：120-160ms。
- 禁止循环装饰动画。
- `prefers-reduced-motion` 下移除位移动画，仅保留必要的状态变化。

## 验收

- 桌面与移动端同时截图检查。
- 检查对比度、溢出、焦点、键盘操作、加载、空、错误和禁用状态。
- 集中修复后最多再确认一轮。
- 页面可见文案不使用破折号作为装饰分隔。

